<?hsp
include("/api/v2/includes/main.hsp");

var index = getPost()['index'];
if (index != null) {
//    var pre = db.prepare("SELECT `author` FROM `axda-plugin` WHERE `index`=? LIMIT 1;");
//    pre.set(1, index);
//    var r = pre.executeQuery();
    if (!isOwner(index)) {
        data.code = 4030;
    } else {
        data.code = 2000;
        var app = module("App");
        app.delete(index);
    }
} else {
    data.code = 4031;
}
echo(data);